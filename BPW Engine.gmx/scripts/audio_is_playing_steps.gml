return audio_is_playing(snd_boardstep06) || audio_is_playing(snd_dirtstep00) ||
    audio_is_playing(snd_dirtstep01) || audio_is_playing(snd_woodstep00) || audio_is_playing(snd_woodstep01)
    || audio_is_playing(snd_woodstep02) || audio_is_playing(snd_boardstep00) ||
    audio_is_playing(snd_boardstep01) || audio_is_playing(snd_boardstep02) || audio_is_playing(snd_boardstep03)
    || audio_is_playing(snd_boardstep04) || audio_is_playing(snd_boardstep05) ||
    audio_is_playing(snd_dirtstepnew00) || audio_is_playing(snd_dirtstepnew01)
    || audio_is_playing(snd_dirtstepnew02) || audio_is_playing(snd_dirtstepnew03);
